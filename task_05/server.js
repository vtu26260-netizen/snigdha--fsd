function transfer(user, merchant, amount, shouldFail=false){let result={userBefore:5000, merchantBefore:2000};try{if(shouldFail) throw new Error('Network failure');result.userAfter=result.userBefore-amount;result.merchantAfter=result.merchantBefore+amount;result.status='COMMIT successful';}catch(e){result.userAfter=result.userBefore;result.merchantAfter=result.merchantBefore;result.status='ROLLBACK executed';}return result;}console.log(transfer(1,2,500));